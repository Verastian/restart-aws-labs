services:
  docs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: restart-labs-docs
    ports:
      - "8080:80"
    restart: always
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost/"]
      interval: 30s
      timeout: 10s
      retries: 3
